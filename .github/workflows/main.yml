name: spider
on: [push]

jobs:
  spider:
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        n: [1,2,3,4]
    steps:
    - uses: actions/checkout@v3
    - run: chmod +x spider
    - run: sudo ./spider 91.108.17.36 32000 30
